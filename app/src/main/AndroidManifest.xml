<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.rahul.videostreamer">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_play"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_play"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="in.rahul.videostreamer.view.activity.VideoPlayerActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name="in.rahul.videostreamer.view.activity.VideoAllActivity" />
        <activity android:name="in.rahul.videostreamer.view.activity.LoginActivity" />
        <activity android:name="in.rahul.videostreamer.view.activity.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
    </application>

</manifest>